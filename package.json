{
  "name": "cafe-management",
  "private": true,
  "proxy": "http://localhost:5000",
  "workspaces": [
    "client/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^1.12.0"
  }
}
